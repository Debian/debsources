#!/bin/bash

cat << EOT

Make debsources up-to-date in the container:
============================================

bin/debsources-docker-update

Other infos:
============

Navigate the webapp in your browser at http://localhost:5000

EOT

docker-compose up --no-recreate
